{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug videocut (MinGW)",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}\\build\\videocut.exe",
      "args": [],
      "stopAtEntry": false,
      "cwd": "${workspaceFolder}",
      "environment": [
        {
          "name": "PATH",
          "value": "${env:QT_SDK_DIR}/bin;${env:QT_MINGW_DIR}/bin;${workspaceFolder}/build-release;${env:PATH}"
        }
      ],
      "externalConsole": false,
      "MIMode": "gdb",
      "miDebuggerPath": "${env:QT_MINGW_DIR}/bin/gdb.exe",
      "setupCommands": [
        {
          "description": "Enable pretty-printing",
          "text": "-enable-pretty-printing",
          "ignoreFailures": true
        }
      ],
      "preLaunchTask": "Build (Debug)"
    }
  ]
}
